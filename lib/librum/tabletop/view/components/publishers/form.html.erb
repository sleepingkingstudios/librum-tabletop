<%= render_form do %>
  <%= render_form_field('publisher[name]') %>

  <%= render_form_field('publisher[slug]') %>

  <%= render_form_field('publisher[website]') %>

  <%= render_form_actions %>
<% end %>
